describe('line count can be checked', () => {
  // const filespec = 'server/async-01-line-count-with-callback/line-count.js';

  test.todo('with the done parameter like with a callback')

  test.todo('by returning the promise after .then')

  test.todo('by using eventually or fulfilledWith')
})
